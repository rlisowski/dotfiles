project_name: duty
project_root: /home/rlisowski/workspace/efiduty
windows:
  - zsh:
    - gv .
    - gst
  - server:
    - vagrant up postgres rabiitmq
    - if ( be passenger status | grep -q "running on" ) then ( be passenger stop ) fi; be passenger start -e vagrant
